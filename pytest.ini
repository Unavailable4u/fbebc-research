[pytest]
# tests/adv is host-only (no Docker needed) so it's safe in the default run,
# same as tests/unit. tests/integration is deliberately NOT listed here --
# it needs a live Docker daemon and is meant to be run explicitly
# (`pytest tests/integration -v`), per WEEK2_SETUP.md.
testpaths = tests/unit tests/adv
